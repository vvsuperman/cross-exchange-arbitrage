python-dotenv>=1.0.0
pytz>=2025.2
# bpx-py 等要求 requests>=2.31；钉在 2.33 线便于与若干子依赖一致
requests~=2.33.1
# 与 requests（idna<4,>=2.5）等组合兼容，减少 pip 回溯
idna>=3.11,<4
tenacity>=9.1.2
flask>=2.0.0
pandas>=1.3.0
numpy>=1.21.0
bpx-py==2.0.11
redis>=5.0.0
# 监控/WS 等（Lighter 见 requirements-lighter.txt + lighter_env）
websockets>=16.0
aiohttp>=3.9.0
aiohttp-retry>=2.9.1
# 浏览器自动化（exchanges/frontend_arbitrage.py 等）；Lighter 已拆到 lighter_env，主环境可共用 urllib3 新版
seleniumbase>=4.47.9
matplotlib>=3.5.0

# 说明：plot_spread.py 仍依赖 matplotlib，如不使用该脚本可移除

# Forked edgeX official python sdk with post_only limit order
git+https://github.com/your-quantguy/edgex-python-sdk.git@7ce85b6ebfa9df0a6f6252d93e10d106927257a9#egg=edgex-python-sdk

# Lighter：独立 venv + requirements-lighter.txt（避免 urllib3 与 seleniumbase 等冲突）